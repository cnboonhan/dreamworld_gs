diff -ruN '--exclude=.git' '--exclude=glm' '--exclude=recastnavigation' '--exclude=__pycache__' '--exclude=*.egg-info' '--exclude=build' hyw-clean/hyworld2/worldgen/models/worldstereo_wrapper.py b/hyworld2/worldgen/models/worldstereo_wrapper.py
--- hyw-clean/hyworld2/worldgen/models/worldstereo_wrapper.py	2026-08-04 01:07:13.669537869 +0000
+++ b/hyworld2/worldgen/models/worldstereo_wrapper.py	2026-08-04 00:57:37.927236977 +0000
@@ -165,6 +165,18 @@
             )
 
         cfg = OmegaConf.create(cls._load_hf_config(json_cfg_path))
+
+        # Resolve the base model repo id to its local snapshot directory.
+        # Several of its subfolders (notably tokenizer/) lack a config.json;
+        # with a repo id, transformers tries the hub for the missing file and
+        # hard-fails under HF_HUB_OFFLINE instead of falling back. A local
+        # directory path skips hub resolution entirely.
+        if local_files_only and not os.path.isdir(str(cfg.base_model)):
+            from huggingface_hub import snapshot_download
+
+            cfg.base_model = snapshot_download(
+                str(cfg.base_model), local_files_only=True
+            )
         model_weights_path = safetensors_path
 
         model_type = subfolder
@@ -182,6 +194,7 @@
             fsdp=fsdp,
             device_mesh=device_mesh,
             device=device,
+            local_files_only=local_files_only,
         )
 
         text_encoder, image_clip, vae = cls._load_aux(
@@ -249,6 +262,7 @@
         fsdp: bool,
         device_mesh,
         device,
+        local_files_only: bool = False,
     ):
 
         half_dtype = _get_half_dtype()
@@ -260,6 +274,7 @@
                 subfolder="transformer",
                 controlnet_cfg=cfg.controlnet_cfg,
                 torch_dtype=half_dtype,
+                local_files_only=local_files_only,
             )
         else:
             transformer = WorldStereoRefSModel.from_pretrained(
@@ -267,6 +282,7 @@
                 subfolder="transformer",
                 controlnet_cfg=cfg.controlnet_cfg,
                 torch_dtype=half_dtype,
+                local_files_only=local_files_only,
             )
 
         rank0_log("Building ControlNet…")
diff -ruN '--exclude=.git' '--exclude=glm' '--exclude=recastnavigation' '--exclude=__pycache__' '--exclude=*.egg-info' '--exclude=build' hyw-clean/hyworld2/worldgen/src/retrieval_wm.py b/hyworld2/worldgen/src/retrieval_wm.py
--- hyw-clean/hyworld2/worldgen/src/retrieval_wm.py	2026-08-04 01:07:13.673537913 +0000
+++ b/hyworld2/worldgen/src/retrieval_wm.py	2026-08-03 23:37:29.925146322 +0000
@@ -822,8 +822,8 @@
         rank0_log(f"Initializing SAM3 Model...")
         if sam3_model is None or sam3_processor is None:
             from transformers import Sam3VideoModel, Sam3VideoProcessor
-            self.sam3_model = Sam3VideoModel.from_pretrained("facebook/sam3").to(device, dtype=torch.bfloat16)
-            self.sam3_processor = Sam3VideoProcessor.from_pretrained("facebook/sam3")
+            self.sam3_model = Sam3VideoModel.from_pretrained(os.environ.get("SAM3_VIDEO_DIR", "/assets/models/sam3_video")).to(device, dtype=torch.bfloat16)
+            self.sam3_processor = Sam3VideoProcessor.from_pretrained(os.environ.get("SAM3_VIDEO_DIR", "/assets/models/sam3_video"))
         else:
             self.sam3_model = sam3_model
             self.sam3_processor = sam3_processor
Binary files hyw-clean/hyworld2/worldgen/third_party/gsplat_maskgaussian/gsplat/csrc.so and b/hyworld2/worldgen/third_party/gsplat_maskgaussian/gsplat/csrc.so differ
diff -ruN '--exclude=.git' '--exclude=glm' '--exclude=recastnavigation' '--exclude=__pycache__' '--exclude=*.egg-info' '--exclude=build' hyw-clean/hyworld2/worldgen/traj_generate.py b/hyworld2/worldgen/traj_generate.py
--- hyw-clean/hyworld2/worldgen/traj_generate.py	2026-08-04 01:07:13.677537959 +0000
+++ b/hyworld2/worldgen/traj_generate.py	2026-08-03 23:58:51.655693179 +0000
@@ -73,11 +73,12 @@
 
 SAM_BATCH_SIZE = 4
 MAX_MASK_COUNT = 5
-HF_CACHE_DIR = os.path.expanduser("~/.cache/huggingface/hub")
+HF_CACHE_DIR = os.environ.get("HF_HUB_CACHE") or os.path.join(
+    os.environ.get("HF_HOME", os.path.expanduser("~/.cache/huggingface")), "hub")
 ZIM_REPO_ID = "naver-iv/zim-anything-vitl"
 ZIM_SUBFOLDER = "zim_vit_l_2092"
 GD_REPO_ID = "IDEA-Research/grounding-dino-tiny"
-SAM3_REPO_ID = "facebook/sam3"
+SAM3_REPO_ID = os.environ.get("SAM3_IMAGE_DIR", "/assets/models/sam3_image")
 MOGE_ID = "Ruicheng/moge-2-vitl-normal"
 
 
@@ -204,8 +205,8 @@
 
     # VLM & SAM3
     client = OpenAI(api_key="EMPTY", base_url=f"http://{LLM_ADDR}:{LLM_PORT}/v1")
-    sam3_model = Sam3Model.from_pretrained("facebook/sam3").to(device)
-    sam3_processor = Sam3Processor.from_pretrained("facebook/sam3")
+    sam3_model = Sam3Model.from_pretrained(os.environ.get("SAM3_IMAGE_DIR", "/assets/models/sam3_image")).to(device)
+    sam3_processor = Sam3Processor.from_pretrained(os.environ.get("SAM3_IMAGE_DIR", "/assets/models/sam3_image"))
     print("Models Initializing over.")
 
     # Near-view rotations used by regular trajectory generation.
diff -ruN '--exclude=.git' '--exclude=glm' '--exclude=recastnavigation' '--exclude=__pycache__' '--exclude=*.egg-info' '--exclude=build' hyw-clean/hyworld2/worldgen/video_gen.py b/hyworld2/worldgen/video_gen.py
--- hyw-clean/hyworld2/worldgen/video_gen.py	2026-08-04 01:07:13.677537959 +0000
+++ b/hyworld2/worldgen/video_gen.py	2026-08-03 23:37:18.505011073 +0000
@@ -23,7 +23,7 @@
 os.environ["TOKENIZERS_PARALLELISM"] = "false"
 timer = Timer()
 
-SAM3_REPO_ID = "facebook/sam3"
+SAM3_REPO_ID = os.environ.get("SAM3_VIDEO_DIR", "/assets/models/sam3_video")
 MOGE_ID = "Ruicheng/moge-2-vitl-normal"
 
 if __name__ == '__main__':
