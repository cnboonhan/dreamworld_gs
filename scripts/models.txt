# HuggingFace repos the pipeline loads, fetched by `just fetch`.
# Blank lines and #-comments ignored. Every entry is a verified runtime load;
# an audit against the pipeline found (and removed) 138 GB that nothing loads.

# the panorama editor's model — variants (a look) from a prompt
# (~54GB on disk, bf16 needs ~45GB VRAM)
Qwen/Qwen-Image-Edit-2509

# the mission agent's VLM (the harness at /harness)
Qwen/Qwen3-VL-8B-Instruct

# edge transitions: first+last-frame video generation (door openings, lift
# rides) — ~118GB, a two-expert MoE
Wan-AI/Wan2.2-I2V-A14B-Diffusers

# the live layer's default: the distilled causal checkpoint that holds a
# view still (~81GB). DW_STREAMER_RUNNER names which one serves.
robbyant/lingbot-world-v2-14b-causal-fast

# The UMT5 text encoder the LingBot runners prompt through. A DIFFERENT
# repo from the Wan2.1-T2V-1.3B below despite the near-identical name —
# the -Diffusers one is the only place flashdreams looks for
# text_encoder/ and tokenizer/, and without it the streamer loads its
# weights, then dies reaching for a text encoder that is not there.
# Only text_encoder/** and tokenizer/** are actually read (~22GB).
Wan-AI/Wan2.1-T2V-1.3B-Diffusers

# the live layer's small alternative — Causal-Forcing Wan 2.1 1.3B I2V.
# Also baked into the streamer image, so a box that builds it locally
# already has these; listed because a box that PULLS the image does not.
zhuhz22/Causal-Forcing
Wan-AI/Wan2.1-T2V-1.3B
lightx2v/Autoencoders
